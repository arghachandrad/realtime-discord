import { styled } from "@mui/system"

const MainContainer = styled("div")({
  flexGrow: 1,
  backgroundColor: "#363937",
  marginTop: "48px",
  display: "flex",
})

function Messenger() {
  return <MainContainer>Messenger</MainContainer>
}

export default Messenger
